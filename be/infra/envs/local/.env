DB_USERNAME=root
DB_PASSWORD=root_password
DB_HOST=localhost:3306
DB_NAME=mishis4x
ENV=local
# 1 is the seeded test/test user's ID (see be/db/seeds/001_users_seed.sql) -
# matches locally out of the box. A real deployment sets this to whichever
# users.id is actually allowed to see collection-tracker routes; see
# handlers.Data.CollectionOwnerUserID's doc comment for why this exists.
COLLECTION_OWNER_USER_ID=1
# Local-only convenience: lets any signed-up account see collection-tracker
# routes instead of just the one above, since nothing served locally is
# real eBay-sourced data. Deliberately NOT set in compose.yaml - see
# handlers.Data.CollectionAllowAllUsers's doc comment.
COLLECTION_ALLOW_ALL_USERS=true
# 1 is the seeded test/test user's ID, same as COLLECTION_OWNER_USER_ID
# above - lets that account see /api/admin/... routes locally. See
# handlers.Data.AdminUserID's doc comment for why this is a separate
# config value from COLLECTION_OWNER_USER_ID despite matching it here.
ADMIN_USER_ID=1
# 1 is the seeded test/test user's ID, same as ADMIN_USER_ID above - lets
# that account see /api/models/... routes locally. See
# handlers.Data.ModelViewerUserID's doc comment for why this is its own
# config value rather than a reuse of ADMIN_USER_ID/
# COLLECTION_OWNER_USER_ID despite matching them here.
MODEL_VIEWER_USER_ID=1